[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = [
  "npm run build",
  "node scripts/prerender.mjs"
]

[start]
cmd = "npx -y serve@latest -s dist/client -l tcp://0.0.0.0:${PORT:-3000}"
